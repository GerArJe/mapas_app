import 'package:flutter/material.dart';

class AccesoGpsPage extends StatelessWidget {
  const AccesoGpsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AccesoGpsPage'),
      ),
    );
  }
}
